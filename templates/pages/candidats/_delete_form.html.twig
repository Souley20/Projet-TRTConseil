<form method="post" action="{{ path('app_candidats_delete', {'id': candidats.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ candidats.id) }}">
	<button class="btn btn-danger btn-lg mt-4">Supprimer</button>

</form>