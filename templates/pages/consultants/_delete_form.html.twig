<form method="post" action="{{ path('app_consultants_delete', {'id': consultants.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ consultants.id) }}">
	<button class="btn btn-secondary btn-lg mt-4">Supprimer</button>

</form>
